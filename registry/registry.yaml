schema_version: "1.0.0"

# Curated list of official and known ATDD artifacts: extensions (use-case
# packages) and workspace providers (reusable runtimes).
registry_id: atdd.artifact-registry
entries:
  - entries/atdd.workspace.python-pytest.yaml
  - entries/atdd.extension.github.yaml
  - entries/acme.extension.component-header-validator.yaml
