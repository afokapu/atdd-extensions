# ATDD Extension Registry

This directory lists official and known external ATDD extensions.

The registry is not required to create or install an extension.

It is used for discovery, review, and ecosystem indexing.

## Rules

- Official extensions use `atdd.extension.*`.
- External extensions use their own publisher namespace.
- Registry listing does not imply official ATDD ownership unless the extension uses the `atdd` namespace.
