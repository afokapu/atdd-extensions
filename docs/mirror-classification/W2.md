# W2 mirror classification — Train (composition root + journey tests)

Worker: **W2**
Core source (read-only): `/Users/alecfokapu/Github/atdd/main/src/atdd/`
Build target (this repo): `official/convex.workspace.runtime` (detectors),
`official/convex.extension.coder` + `official/convex.extension.tester` (convention nodes).

Trains are a **backend production-orchestration** concern (composition roots, wagons,
cargo, Station Master, TrainRunner). Journey tests are the **assembly-layer test**
surface for those trains. Every agnostic rule is therefore mirrored onto the
**convex** stack (backend) — none are frontend-only, so none are dual-mirrored.

Grounded against real consumer code:
`/Users/alecfokapu/Github/frg-app/main/apps/game/convex/` — wagon dirs
(`run_match/`, `score_grid/`, `setup_match/`, …), composition root `run_match/wagon.ts`
("the ONE place run-match is allowed to reach across a wagon boundary … the wagon
order of the 3003-reveal-round train is defined HERE, as data, not in import
statements"), and journey/lifecycle tests (`run-match.drive-match.test.ts`).

---

## Legend
- **AGNOSTIC-CONSUMER** → obligation binds any consumer repo's code → **MIRROR** (detector + node + fixtures + test).
- **ATDD-INTERNAL / SUBSTRATE-SPEC** → governs the ATDD toolkit's own substrate or is a pending internally-owned validator → **DOCUMENT WHY-NOT** (no mirror).

---

## coder — `coder/conventions/train.convention.yaml` (3 structured rules)

| core rule_id | classification | mirrored rule_id | detector (impl) |
|---|---|---|---|
| `coder.train.is-a-production` | **AGNOSTIC-CONSUMER** | `coder.convex.train-is-a-production` | `convex_train_composition_detector` (family) |
| `coder.train.wagons-communicate-via-cargo` | **AGNOSTIC-CONSUMER** | `coder.convex.train-wagons-communicate-via-cargo` | `convex_train_composition_detector` (family) |
| `coder.train.yaml-is-the` | **AGNOSTIC-CONSUMER** | `coder.convex.train-yaml-source-of-truth` | `convex_train_composition_detector` (family) |

### `coder.train.is-a-production` → AGNOSTIC-CONSUMER
> "Train is a production composition root in python/trains/, not test infrastructure"
> rationale: "Trains are PRODUCTION orchestration, not test infrastructure. They execute user journeys …"

The obligation ("orchestration composition roots are *production* code, not test
infrastructure") binds any consumer repo. `python/trains/` is merely the Python
*rendering* of the composition-root location; the Convex rendering is a
`TrainRunner`/composition-root **module** that must not be authored as, or buried
under, test infrastructure (`*.test.ts`, `e2e/`, `__tests__/`). Mirrored: the
detector flags a train-runner **definition** that lives in a test file/dir.

### `coder.train.wagons-communicate-via-cargo` → AGNOSTIC-CONSUMER
> "Wagons communicate via cargo (contract artifacts) — never via direct cross-wagon imports"
> `boundary_enforcement.no_direct_imports.rule`: "Wagons NEVER import from other wagons"

A pure consumer-code boundary rule — realized 1:1 in frg-app, where `run_match/wagon.ts`
is *"the ONE place run-match is allowed to reach across a wagon boundary"* (the
composition-root exception). Mirrored: the detector flags a cross-wagon import
(`@<other-wagon>/wagon` or `../<other-wagon>/…`) from any non-composition-root
module.

### `coder.train.yaml-is-the` → AGNOSTIC-CONSUMER
> "Train YAML is the single source of truth for orchestration; production and tests both consume it via TrainRunner"
> `station_master` anti-pattern: "Business logic in app.py (belongs in trains/wagons)"

frg-app states the invariant directly: *"the wagon order of the 3003-reveal-round
train is defined HERE, as data, not in import statements."* Mirrored: the detector
flags a Station Master (`app.ts` / `station-master.ts`) that hardcodes a multi-wagon
call sequence inline instead of delegating to a declarative train spec via a
`TrainRunner` / journey-id map.

---

## coder — `coder/conventions/nodes/coder.train.*.convention.yaml` (2 node rules)

| core rule_id | classification | why-not |
|---|---|---|
| `coder.train.acceptance-commit-idempotent` | **ATDD-INTERNAL / SUBSTRATE-SPEC** | Acceptance Authority/Registry substrate; pending internal validator |
| `coder.train.station-master-owns-child-train-fanout` | **ATDD-INTERNAL / SUBSTRATE-SPEC** | Station-Master fan-out + durable run-log substrate; pending internal validator |

### `coder.train.acceptance-commit-idempotent` → WHY-NOT (SUBSTRATE-SPEC / PLANNER-OWNED)
> "An Acceptance Authority commit MUST be idempotent … the Acceptance Registry is the durable source of truth. **Pending binding - validator owned by D3 (#1087).**"
> term `commit-idempotency`: "atomic compare-and-set on expected_version + idempotency_key; replay-safe"

This governs the ATDD **Acceptance Authority / Acceptance Registry** commit
protocol — toolkit substrate, not consumer application code — and is an explicitly
**pending, internally-owned** validator (`owned by D3 (#1088)`). No consumer-stack
realization exists to mirror. **Not mirrored.**

### `coder.train.station-master-owns-child-train-fanout` → WHY-NOT (SUBSTRATE-SPEC)
> "Acceptance-set fan-out is owned by the Station Master (parent suspend/resume) … **suspend/resume state lives in the durable run-log, not in TrainRunner. Pending binding - validator owned by D4 (#1088).**"
> term `fanout-ownership`: "Station Master fans out child behavior-train runs and aggregates; TrainRunner stays one linear executor"

The core of this rule is the ATDD runtime's own **suspend/resume + durable run-log**
fan-out protocol between the Station Master and TrainRunner substrate — and it is an
explicitly **pending, internally-owned** validator (`owned by D4 (#1088)`). The one
consumer-facing sub-clause ("a wagon MUST NOT … perform fan-out / reach the runner")
is already covered by the mirrored `coder.convex.train-wagons-communicate-via-cargo`
boundary. **Not mirrored** (substrate + pending internal owner).

---

## tester — `tester/conventions/train.convention.yaml`

The journey-test **header** contract is agnostic — the convention itself ships a
**TypeScript** rendering, which is the decisive AGNOSTIC signal:
> ```
> // URN: test:train:3001-solo-match-complete:E2E-001-match-lifecycle
> // Train: train:3001-solo-match-complete
> // Phase: RED
> // Layer: assembly
> // Runtime: supabase
> ```

| core obligation (source) | classification | mirrored rule_id | detector (impl) |
|---|---|---|---|
| `required_header` — "Train: header MUST reference a valid train URN" | **AGNOSTIC-CONSUMER** | `tester.convex.journey-train-header` | `convex_journey_test_detector` (family) |
| `urn_format.pattern` — `test:train:{train_id}:{HARNESS}-{NNN}-{slug}` | **AGNOSTIC-CONSUMER** | `tester.convex.journey-urn-format` | `convex_journey_test_detector` (family) |
| `required_header` — "Layer MUST be 'assembly' for journey tests" | **AGNOSTIC-CONSUMER** | `tester.convex.journey-layer-assembly` | `convex_journey_test_detector` (family) |
| `mutual_exclusion` — "MUST NOT have Acceptance: or WMBT:" | **AGNOSTIC-CONSUMER** | `tester.convex.journey-no-acceptance-marker` | `convex_journey_test_detector` (family) |
| `tester.train.coverage` — "Each train wagon has a smoke test exercising its composition root" | **AGNOSTIC-CONSUMER** | `tester.convex.train-coverage` | `convex_train_coverage_detector` (singleton) |

### journey header family → AGNOSTIC-CONSUMER
> `required_header.rules`:
> - "Train: header MUST reference a valid train URN"
> - "Train URN MUST match pattern: train:{NNNN}-{kebab-case}"
> - "Layer MUST be 'assembly' for journey tests"
> - "Acceptance: and WMBT: lines MUST NOT appear in journey tests"
> `validation_rules`: "Train: header must reference a valid train URN matching pattern `train:\d{4}-[a-z0-9][a-z0-9-]*`"

Per-file, header-only obligations on any consumer's assembly-layer journey tests.
Mirrored as a four-member family (`*.test.ts` gate = a header carrying a
`test:train:` URN or a `Train:` marker).

### `tester.train.coverage` → AGNOSTIC-CONSUMER
> "Each train wagon has a smoke test exercising its composition root" (severity 2)

A consumer test-suite completeness obligation. Mirrored: the detector reads a train
spec's declared `wagons` and flags any declared wagon with no `*smoke*.test.ts`
exercising it.
