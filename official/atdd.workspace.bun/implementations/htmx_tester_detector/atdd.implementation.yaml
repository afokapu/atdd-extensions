schema_version: 1.1.0
kind: implementation
subtype: validator
implementation_id: htmx_tester_detector
targets_workspace: atdd.workspace.bun
contract_version: 1.1.0
# The htmx-specific half of the tester surface, split out from
# bun_tester_discipline_detector so each implementation family maps to exactly one
# extension. Bun is the RUNTIME; htmx is a library layered on it, and a Bun repo
# serving JSON wants the Bun tester rules and none of these.
emits_rule_ids:
- tester.htmx.fragment-asserts-markup
entrypoint: detect.mjs
report: detect.mjs
realizes_convention:
- tester.htmx.fragment-asserts-markup
