# FAIL-CLOSED (layout_unresolved): this repo DECLARES an interlocking route space, but its train
# runtime and Station Master do not live where the resolved layout looks (here: nowhere — there is no
# python/ tree at all). Under the old detector this scanned SILENTLY green: none of the runtime- or
# station-facing binding directions could run, so the interlocking looked "clean" while never having
# been bound to any executable runtime. The layout_unresolved tooth turns that silent no-op into ONE
# violation. The route's train artifact exists and the interlocking is not exposed, so no other
# direction fires — layout_unresolved is emitted alone.
schema_version: 1.0.0
interlocking_id: interlocking:match-resolution
title: Match resolution route interlocking
theme: match
status: checked

entrypoint:
  surfaces: [backend]
  exposed: false
  actions: []
  reason: internal-transition-only

route_resolution:
  strategy: fail_on_multiple_match

routes:
  - route_id: nominal-all-voted
    priority: 1
    projection:
      expected_sequence_digest: "d285d26427cb21fedb4c5b56edc673743f6d63c85686bdeb37f5e8c5ed4736d8"
    category: nominal
    guard_ref: guard:all-voted
    train_id: 3007-match-resolution-standard
    train_path: plan/_trains/3007-match-resolution-standard.yaml

lifelines:
  - ref: user:actor
  - ref: wagon:match-resolution
messages:
  - id: msg:match-resolution-request
    kind: boundary
    from: user:actor
    to: wagon:match-resolution
    intent: The actor requests Match Resolution
    payload:
      contract: match-resolution:request-v1

source:
  path: official/atdd.extension.coder.train-interlocking/implementations/interlocking-binding/fixtures/fail/layout_unresolved_no_runtime_or_station/plan/_trains/_interlockings/match-resolution.yaml
  content_digest: "fa4bc780c6c49bcf10f6cd1a807a4a59257e8a29433f621876a856d82ca77c8c"
