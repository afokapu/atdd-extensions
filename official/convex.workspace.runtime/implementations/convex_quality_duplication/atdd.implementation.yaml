schema_version: "1.1.0"
kind: implementation
implementation_id: coder.convex.quality-duplication
targets_workspace: convex.workspace.runtime
contract_version: "1.1.0"

# Convex-workspace realization of coder.convex.quality-duplication, adapted from
# Core's coder.refactor.quality-duplication (AST-window → token-window, no TS runtime).
realizes_convention: coder.convex.quality-duplication

emits_rule_ids:
  - coder.convex.quality-duplication   # disposition: strict

entrypoint: detect.mjs
report: detect.mjs
