// URN: component:orders:order:OrderController:backend:presentation
// Runtime: convex
// Suffix '-controller' is a presentation component type and the file lives under
// presentation/ — suffix matches layer.
export async function handleCreateOrder(req: unknown): Promise<unknown> {
  return req
}
