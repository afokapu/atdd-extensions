// Beta domain helper with a different comment header entirely.
export function compute(input: number): number {
  const a = input + 1;
  const b = a * 2;
  const c = b - 3;
  const d = c / 4;
  const e = d % 5;
  const f = e + 6;
  return f;
}
