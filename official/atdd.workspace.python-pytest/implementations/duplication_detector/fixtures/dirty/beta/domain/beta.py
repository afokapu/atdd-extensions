"""Beta module with a longer distinct docstring header."""
import os
import sys


def process(data):
    step_one = data + 1
    step_two = step_one * 2
    step_three = step_two - 3
    step_four = step_three / 4
    result = step_four
    return result
