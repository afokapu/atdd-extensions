schema_version: "1.1.0"
kind: implementation

# SCAFFOLD (afokapu/atdd-extensions#24). Manifest + skeleton only; the detector
# under src/ is a NotImplementedError stub that #25 fills. The implementation_id,
# provider target, contract range, realized convention, and report channel are the
# package contract #25 builds against — agreed here so #25 does not redesign them.
implementation_id: coder.interlocking.runner-infrastructure.impl
targets_workspace: atdd.workspace.python-pytest
contract_version: "1.1.0"

# Back-reference to the agnostic obligation this detector realizes. The convention
# node is authored by #25 in ../../conventions/; this is its python-pytest
# realization (two-layer model). Dangling until #25 lands the node.
realizes_convention: coder.interlocking.runner-infrastructure

# Single strict rule_id. The detector emits RAW violations; the strict verdict is
# a downstream consumer concern. #25 confirms/extends.
emits_rule_ids:
  - coder.interlocking.runner-infrastructure   # disposition: strict

# The pure detector module (#25 implements). Proves the consumer runtime ships an
# InterlockingRunner route-control layer wired into Station Master -> TrainRunner
# (core afokapu/atdd#1251). Scans the scope's python_runtime + station_master
# selectors.
entrypoint: src/interlocking_infrastructure.py

# The runnable v1.1 report-emitting pytest the provider CLI collects: scans
# ATDD_SCAN_ROOTS and writes the RAW {rule_id,file,line,col,evidence,source_line}
# report to ATDD_VIOLATIONS_REPORT. #25 implements; stub for now.
report: tests/test_interlocking_infrastructure.py
