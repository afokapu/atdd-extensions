# CLEAN fixture — wagons in FLOW style.
#
# Only block style was parsed, so a train's wagons came back EMPTY and the rule
# passed by having nothing to check. Flow sequences are ordinary YAML; this is
# the fifth copy of the same defect across this hub's hand-rolled scanners.
# This tree must behave exactly like its block-style sibling.
trains:
  3-match:
    30-match-nominal:
    - train_id: 3001-host-a-match
      description: Host picks a scenario and creates a gold/azur tournament
      wagons: [curate-content, setup-match]
    - train_id: 3002-play-a-match
      description: Play a match through to resolution
      wagons: [run-match]
