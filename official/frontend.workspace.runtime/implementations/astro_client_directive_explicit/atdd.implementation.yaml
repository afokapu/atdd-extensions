schema_version: "1.1.0"
kind: implementation
implementation_id: coder.astro.client-directive-explicit
targets_workspace: frontend.workspace.runtime
contract_version: "1.1.0"

realizes_convention: coder.astro.client-directive-explicit

# Single rule_id (disposition suppress-and-clean, severity 2). RAW emit only.
emits_rule_ids:
  - coder.astro.client-directive-explicit   # disposition: suppress-and-clean

# Scans `.astro` markup for React-style `on*={…}` handlers whose owning element has
# no `client:*` directive (dead, never-hydrated interactivity).
entrypoint: detect.mjs
report: detect.mjs
