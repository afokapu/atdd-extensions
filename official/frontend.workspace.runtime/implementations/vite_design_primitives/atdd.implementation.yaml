schema_version: "1.1.0"
kind: implementation
implementation_id: coder.vite.design-primitives
targets_workspace: frontend.workspace.runtime
contract_version: "1.1.0"

realizes_convention: coder.vite.design-primitives

emits_rule_ids:
  - coder.vite.design-primitives   # disposition: strict

# The detector module. Scans ATDD_SCAN_ROOTS for *.tsx app code and flags each raw
# interactive HTML element (<button/<input/<select/<textarea). Writes the RAW v1.1
# report to ATDD_VIOLATIONS_REPORT.
entrypoint: detect.mjs

report: detect.mjs
