---
// DIRTY: credentials hardcoded into the component-script fence.
// Placeholder values only — no real provider sub-format (push-protection safe).
const apiKey = "sk_REDACTED_PLACEHOLDER_VALUE";
const dbPassword = "hunter2-not-a-real-password-placeholder";
const clientSecret = import.meta.env.OAUTH_CLIENT_SECRET;
const title = "Forge 100";
---

<section>
  <h1>{title}</h1>
  <p data-has-key={Boolean(apiKey)}>configured</p>
  <p>{dbPassword ? "db ready" : "no db"}</p>
  <p>{clientSecret ? "oauth ready" : "no oauth"}</p>
</section>
