schema_version: "1.1.0"
kind: implementation
implementation_id: coder.vite.design-orphan-ui
targets_workspace: frontend.workspace.runtime
contract_version: "1.1.0"

# Back-reference to the agnostic obligation this detector realizes. The convention
# node is authored in frontend.extension.vite-coder; this implementation is the
# Vite/React-workspace realization of it (two-layer model).
realizes_convention: coder.vite.design-orphan-ui

emits_rule_ids:
  - coder.vite.design-orphan-ui   # disposition: strict

# The detector module. Two-pass cross-file scan over ATDD_SCAN_ROOTS: collect all
# imported identifiers, then flag each exported PascalCase .tsx component whose name
# is imported nowhere. Writes the RAW v1.1 report to ATDD_VIOLATIONS_REPORT.
entrypoint: detect.mjs

report: detect.mjs
