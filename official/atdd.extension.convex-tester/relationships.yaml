schema_version: "1.0.0"

# Extension-LOCAL relationship graph for atdd.extension.convex-tester.
#
# IMPORTANT (boundary spec §6.2): core↔extension edges are NOT authored here. Only
# extension-internal edges live here. SKELETON: no nodes yet — the Vitest filename
# node (and any siblings) are added as they are ported from the atdd-js draft (see
# PORTING-PLAN.md), each landing with its atdd.workspace.convex detector.
graph_id: atdd.extension.convex-tester.relationships

nodes: []

edges: []
