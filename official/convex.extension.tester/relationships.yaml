schema_version: "1.0.0"

# Extension-LOCAL relationship graph for convex.extension.tester. Every owned node carries
# >=1 edge (no orphans). Core<->extension edges are narrative, not graph edges.
graph_id: convex.extension.tester.relationships

nodes:
  - tester.convex.filename-urn
  - tester.convex.migration-naming

edges:
  - source_ref: tester.convex.migration-naming
    target_ref: tester.convex.filename-urn
    type: relates-to
    reason: "Both derive an artifact's name deterministically from its identity."
