schema_version: "1.0.0"
kind: implementation
implementation_id: github.release.version-decided-drains-to-tag-and-publish.impl
targets_workspace: atdd.workspace.python-pytest
contract_version: "1.0.0"

# The convention (in this same extension) this worker realizes. Core #1172 emits
# the provider-neutral `version_decided` outbox signal; this is the GitHub-side
# consumer that drains it into a git tag + PyPI publish + tag external_ref. The
# extension's relationships own the realizes edge; this is the executable side.
realizes_convention: github.release.version-decided-drains-to-tag-and-publish
entrypoint: release_worker.py
