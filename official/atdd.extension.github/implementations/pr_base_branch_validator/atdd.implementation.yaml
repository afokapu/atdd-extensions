schema_version: "1.0.0"
kind: implementation
implementation_id: github.pr.base-must-be-default-branch.impl
targets_workspace: atdd.workspace.python-pytest
contract_version: "1.0.0"

# Convention node (in this same extension) whose rule_id this implementation
# emits violations under. The extension's relationships own the realizes edge;
# this is the executable side.
realizes_convention: github.pr.base-must-be-default-branch
entrypoint: base_branch.py
